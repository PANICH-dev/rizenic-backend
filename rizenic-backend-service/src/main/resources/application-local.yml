spring:
  config:
    import: optional:file:../.env.local[.properties]
  datasource:
    url: jdbc:postgresql://127.0.0.1:${LOCAL_DB_PORT:55432}/rizenic_db?currentSchema=rizenic_new&sslmode=disable
    username: rizenic
    password: ${LOCAL_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  address: 127.0.0.1
